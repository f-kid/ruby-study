<h2>Input your Comment</h2>

<%
=begin
%>

<% if @comment.errors.any? %>
  <div>
    <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>
    <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%
=end
%>

<%= form_for(@comment, :url=>theme_comments_path(@comment.theme_id)) do |f| %>
<% # form_for (@comment, {:action => "create"}) do |f| %>
  <table>
    <tr><td>Your Name : </td><td><%= f.text_field :c_orner, { size: 30 } %></td></tr>
    <tr><td>Subject : </td><td><%= f.text_field :c_title , { size: 30 } %></td></tr>
    <tr><td>Cooment : </td><td><%= f.text_area :c_body , { cols: 50, rows: 5 } %></td></tr>
    <tr><td></td><td><%= f.submit %></td></tr>
  </table>
<% end %>

